# Proto provenance
#
# determinism/orchestrator/v1/orchestrator.proto — AUTHORED LOCALLY in this
# repo per API.md §1/§7 (plan decision D4; IMPLEMENTATION-PLAN §M0 sanctioned
# fallback). Upstream determinism-proto's orchestrator::v1 is a hand-written
# placeholder with a different StartExperimentRequest shape and no service;
# reconciling and upstreaming this file into control-plane is a disclosed
# follow-up bead at M4 handback.
#
# determinism/inputsynth/v1 — consumed from the control-plane sibling
# checkout via the determinism-proto crate (not vendored here).
